Тема лабы:
👉 параллельное вычисление остатка от деления большого числа, представленного как вектор машинных слов

Проще:

есть очень большое число, которое не влезает в uint64_t

оно хранится как массив IntegerWord[]

нужно вычислить

(dividend % divisor)


делается это параллельно, с использованием OpenMP

измеряется ускорение при увеличении числа потоков

Вот как читать этот проект:

entrypoint.cpp      — main(), тесты + замеры
vector_mod.*        — ГЛАВНАЯ функция (ядро лабы)
mod_ops.*           — безопасная арифметика по модулю
num_threads.*       — управление числом потоков (OpenMP)
performance.*       — замеры времени и ускорения
randomize.*         — параллельная генерация входных данных
test.*              — корректностные тесты (огромные массивы)

В третьей лабораторной работе реализовано параллельное вычисление остатка от деления большого числа, представленного в виде массива машинных слов.
Для предотвращения переполнения используется модульная арифметика.
Вычисление распараллелено с помощью OpenMP, а производительность измеряется при разном числе потоков.
Корректность подтверждается набором тестов, а ускорение оценивается экспериментально.
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
Программа сначала проверяет корректность реализации: функция vector_mod вычисляет остаток от деления большого числа, представленного массивом машинных слов, и сравнивает результат с эталонными значениями из набора тестов. Сообщение Correctness tests. ok. означает, что все тесты пройдены и алгоритм работает правильно.

Далее выполняются измерения производительности. Для одного и того же входного массива вычисление запускается при разном числе потоков OpenMP. В таблице T — количество потоков, Value — результат вычисления остатка (он одинаков для всех запусков, так как входные данные не меняются), Duration — время выполнения в миллисекундах, Acceleration — ускорение относительно однопоточного варианта.

Из результатов видно, что при увеличении числа потоков время выполнения уменьшается, а ускорение растёт почти линейно на малых значениях T. При большом числе потоков рост ускорения замедляется из-за накладных расходов на управление потоками, синхронизацию и ограничений пропускной способности памяти. Это поведение является ожидаемым для реальных параллельных программ и подтверждает эффективность реализованного алгоритма.